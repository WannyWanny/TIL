EC2의 필수 3가지 개념
* AMI(Amazon Machine Image), Security Group, Key Pair

[추후 해결]
* SSH통해 EC2 접속. 계속 port22에러 뜨면서 접속 실패함. 보안문제 때문에 에러가 뜨는게 확실하다.
  지정된 보안그룹은 아무 IP에서 사용하지 못하게 막아둬서 그런지 해결하기가 어려워서 내 계정을 새로 만드는 것으로 일단은 문제 해결.
